[project]
name = "stackview"
version = "0.1.0"
description = "backend service for developer's landing page presentation with full-fledged API and AI tool integration"
authors = [
    {name = "simbarilion",email = "nadezhda13popova@gmail.com"}
]
requires-python = ">=3.12,<3.15"
dependencies = [
    "fastapi (>=0.139.2,<0.140.0)",
    "uvicorn (>=0.51.0,<0.52.0)",
    "poetry-core (>=2.4.1,<3.0.0)",
    "sqlalchemy (>=2.0.51,<3.0.0)",
    "alembic (>=1.18.5,<2.0.0)",
    "asyncpg (>=0.31.0,<0.32.0)",
    "psycopg2-binary (>=2.9.12,<3.0.0)",
    "python-dotenv (>=1.2.2,<2.0.0)",
    "pydantic-settings (>=2.10,<3.0)",
    "aiosmtplib (>=4.0,<5.0)",
    "openai (>=1.99,<2.0)",
    "httpx (>=0.28,<0.29)",
    "jinja2 (>=3.1,<4.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
lint = [
    "ruff (>=0.15.22,<0.16.0)",
    "pre-commit (>=4.6.1,<5.0.0)",
    "mypy (>=2.3.0,<3.0.0)"
]
test = [
    "pytest (>=8.4,<9.0)",
    "pytest-asyncio (>=1.0,<2.0)"
]

[tool.poetry]
package-mode = false

[tool.ruff]
line-length = 119
target-version = "py312"
exclude = [
    ".git",
    "__pycache__",
    "venv",
    ".venv",
    "env",
    ".env",
    "alembic/versions",
    "alembic/env.py",
]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]

ignore = [
    "E203",
    "E701",
    "B008",
]

[tool.ruff.lint.per-file-ignores]
"**/__init__.py" = ["F401"]
"alembic/versions/*.py" = ["E501", "F401", "E402", "W291"]

[tool.ruff.lint.isort]
combine-as-imports = true
split-on-trailing-comma = true

[tool.mypy]
check_untyped_defs = true
disallow_untyped_defs = false
disallow_incomplete_defs = false
disallow_untyped_decorators = false

no_implicit_optional = true
warn_return_any = true
warn_unused_ignores = true

ignore_missing_imports = true

exclude = [
    "venv",
    ".venv",
    "env",
    ".env",
    ".git",
    "__pycache__",
    "^alembic/",
    "^alembic/.*",
    "^tests/",
    ".*\\.env.*",
    "^\\.venv/",
    ".*\\.ini",
    ".*\\.cfg",
    ".*\\.example",
]

[[tool.mypy.overrides]]
module = ["tests.*",]
ignore_errors = true

[tool.poetry.scripts]
seed = "app.scripts.seed:main"

[tool.coverage.run]
source = ["app"]
omit = [
    "app/main.py",
    "app/scripts/*",
    "app/db/migrations/*",
    "app/alembic/*",
    "app/schemas/common.py",
    "app/api/routers/*"
]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["."]
